import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';  // Where we're going to create out front end react app

ReactDOM.render(<App />, document.getElementById('root'));  // Render 'App' into 'document.getElementById('root'))'